var itemRowClass = ".item-model-<%= model_id %>";

$(itemRowClass).replaceWith('<%=j render_item_row(model) %>');

toastr.warning("<%= deactivated_message %>");

blinkHighlight(itemRowClass);

initKrudminScriptsForControls();